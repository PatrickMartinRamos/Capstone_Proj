using UnityEngine;

public class Tutorial : MonoBehaviour
{
    [SerializeField] GameObject promptPanel;
    [SerializeField] GameObject productArea;
    [SerializeField] GameObject constantGear, varGear;
    [SerializeField] GameObject bomb;
    [SerializeField] bool bombSelected=false, gearsCombined=false, toolUsed=false, gearEmbedded=false, verificationPressed = false, bombExploded = false;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
